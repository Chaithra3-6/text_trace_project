services:
  - type: web
    name: text-trace
    env: python
    buildCommand: |
      pip install -r requirements.txt
      gcc text_trace.c -o text_trace
    startCommand: "gunicorn --bind 0.0.0.0:10000 server:app"
